import React from "react";

export default function buttonDate() {
  return (
    <input
      type="date"
      id="appointment-date"
      name="appointmentDate"
      className="selectButton"
    />
  );
}
